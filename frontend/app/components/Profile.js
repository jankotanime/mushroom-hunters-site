'use client';
import "./../globals.css";
import { useEffect, useState } from "react";

const Profile = (props) => {    
  const result = (<div>profil
  </div>)
  return (result);
};

export default Profile;
